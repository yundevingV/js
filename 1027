Prototype(프로토타입)

#Prototype(프로토타입)
#자바스크립트는 프로토타입 기반 언어라고 한다. 
#자바스크립트도 객체지향언어 인데 Class(클래스)라는 개념이 존재하지 않았었고 대신 Prototype(프로토타입)이 존재하기 때문이다.

function Fruit() {
  this.price = 2000;
  this.taste = 'good';
}
let apple = new Fruit();
let banana = new Fruit();

console.log(apple.price);
console.log(banana.price);
console.log(apple.taste);
console.log(banana.taste);

#이렇게 함수를 통해서 Class를 비슷하게 만들 순 있습니다.
#이렇게 되면 객체 100개를 만들면 200개의 변수가 메모리가 할당됩니다.
#이런문제를 프로토타입으로 해결할 수 있습니다.

function Fruit() {}

Fruit.prototype.price = 2000;
Fruit.prototype.taste = 'good';

let apple = new Fruit();
let banana = new Fruit();

console.log(apple.price);
console.log(banana.price);
console.log(apple.taste);
console.log(banana.taste);

#프로토타입을 사용한 코드입니다.
#Fruit 함수로부터 생선된 객체들은 Fruit.prototype 이라는 프로토타입의 값을 모두 가져다 쓸 수 있습니다.

